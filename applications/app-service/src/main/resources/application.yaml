server:
  port: 8081
  error:
    include-message: never
spring:
  application:
    name: "cy-auth"
  devtools:
    add-properties: false
  h2:
    console:
      enabled: true
      path: "/h2"
  profiles:
    include: null

adapters:
  r2dbc:
    host: ${DB_HOST}
    port: ${DB_PORT}
    database: ${POSTGRES_DB}
    schema: ${DB_SCHEMA}
    username: ${POSTGRES_USER}
    password: ${POSTGRES_PASSWORD}

  jwt:
    secret: "Zy+cGYfgATuD3hmeNngokuHwOz8zI1g3ExtzOuNV6ZuOY="
    issuer: ""
    audience: ""
    expires: 3600

springdoc:
  swagger-ui:
    path: "/swagger-ui"

management:
  endpoints:
    web:
      exposure:
        include: "health,prometheus"
  endpoint:
    health:
      probes:
        enabled: true
cors:
  allowed-origins: "http://localhost:4200,http://localhost:8081"

logging:
  level:
    org.springframework.boot.autoconfigure.web.reactive.error.AbstractErrorWebExceptionHandler: OFF
    org.springframework.web.server.adapter.HttpWebHandlerAdapter: ERROR
