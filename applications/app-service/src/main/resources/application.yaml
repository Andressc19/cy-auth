##Spring Configuration
server:
  port: 8080
spring:
  application:
    name: cy-auth
  devtools:
    add-properties: false
  h2:
    console:
      enabled: true
      path: /h2
  profiles:
    include: null

adapters:
  r2dbc:
    host: "localhost"
    port: 3308
    database: "cydb"
    schema: "public"
    username: "test"
    password: "myultr@securep@ssword312!"
